# OS / editor cruft
.DS_Store
Thumbs.db
*.log
_port/
tools/node_modules/
tools/recalls/node_modules/

# Pin images are generated on demand and uploaded to Pinterest as base64 —
# they are never referenced by the site, so hosting them would add ~30MB of
# regenerable binaries to the repo for no reader benefit.
assets/img/pins/
tools/recalls/run.log
